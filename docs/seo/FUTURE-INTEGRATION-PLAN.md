# Future Integration Plan

Current status: `PROHIBITED`.

The authoritative decision record is `seo/production-decisions.json`. Isolated previews are readiness evidence, not deployment artifacts.

## Required Decisions

1. Provide and verify the HTTPS production origin.
2. Select and approve a social-image candidate, composition, and absolute public URL.
3. Resolve the Big Tokyo availability contradiction.
4. Decide search-indexing, AI-search, AI-training, llms.txt, and licensing policies.
5. Confirm language and future visible answer placement decisions.
6. Approve metadata, schema, crawler, sitemap, manifest, and analytics scopes individually.
7. Run production-ready validation against verified values.
8. Receive `APPROVED — ALLOW SEO INTEGRATION INTO WEBSITE`.

## Integration Sequence

Future integration must use a new protected-file baseline, a narrow file-level plan, atomic edits, preview-to-production comparison, tests, and before/after integrity evidence. Preview files must never be copied automatically to the project root or `public/`.

External accounts require separate authorization. Website integration approval does not authorize Google Business Profile, Search Console, analytics, Maps, DataForSEO, Firecrawl, backlink, review-platform, delivery-platform, or hosting actions.
